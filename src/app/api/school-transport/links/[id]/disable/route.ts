import { NextResponse } from "next/server";import{prisma}from"@/lib/db";import{getSession}from"@/lib/auth";import{isSchoolTransportAdmin}from"@/lib/school-transport";
export async function POST(_:Request,c:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});if(!isSchoolTransportAdmin(s.role))return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await c.params;const r=await prisma.schoolTrackingLink.updateMany({where:{id,shopId:s.shopId},data:{isEnabled:false,disabledAt:new Date()}});return r.count?NextResponse.json({success:true}):NextResponse.json({error:"Link not found."},{status:404});}
