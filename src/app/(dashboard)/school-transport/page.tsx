"use client";
import { useCallback,useEffect,useMemo,useState } from "react";import{Copy,Link2,Loader2,RefreshCw}from"lucide-react";import{MapplsLiveMap}from"@/components/maps/MapplsLiveMap";
type Driver={id:string;name:string};type Route={id:string;name:string;description?:string|null;startPointName?:string|null;endPointName?:string|null;isActive:boolean};type LinkRow={id:string;token:string;isEnabled:boolean;routeId?:string|null;route?:{name:string}|null};type Vehicle={id:string;name:string;vehicleNumber?:string|null;driverId?:string|null;driver?:Driver|null;isActive:boolean;trackingLinks:LinkRow[]};type Trip={id:string;status:string;lastLatitude:number|null;lastLongitude:number|null;lastAccuracyM:number|null;lastHeading:number|null;lastLocationAt:string|null;vehicle:{name:string;vehicleNumber:string|null};route:{name:string}|null;driver:{name:string}};
export default function SchoolTransportPage(){const[vehicles,setVehicles]=useState<Vehicle[]>([]),[drivers,setDrivers]=useState<Driver[]>([]),[routes,setRoutes]=useState<Route[]>([]),[trips,setTrips]=useState<Trip[]>([]),[busy,setBusy]=useState(false),[message,setMessage]=useState("");const[vform,setVform]=useState({name:"",vehicleNumber:"",driverId:""}),[rform,setRform]=useState({name:"",startPointName:"",endPointName:"",description:""});
const load=useCallback(async()=>{const[vr,rr,lr]=await Promise.all([fetch("/api/school-transport/vehicles",{cache:"no-store"}),fetch("/api/school-transport/routes",{cache:"no-store"}),fetch("/api/school-transport/live",{cache:"no-store"})]);const[v,r,l]=await Promise.all([vr.json(),rr.json(),lr.json()]);if(vr.ok){setVehicles(v.vehicles??[]);setDrivers(v.drivers??[])}if(rr.ok)setRoutes(r.routes??[]);if(lr.ok)setTrips(l.trips??[])},[]);useEffect(()=>{void load();const id=setInterval(()=>void load(),10000);return()=>clearInterval(id)},[load]);
async function send(url:string,body?:unknown,method="POST"){setBusy(true);setMessage("");const res=await fetch(url,{method,headers:{"Content-Type":"application/json"},body:body?JSON.stringify(body):undefined});const data=await res.json().catch(()=>({}));setBusy(false);if(!res.ok){setMessage(data.error??"Action failed.");return null}await load();return data}
async function addVehicle(e:React.FormEvent){e.preventDefault();if(await send("/api/school-transport/vehicles",vform)){setVform({name:"",vehicleNumber:"",driverId:""});setMessage("Vehicle saved.")}}async function addRoute(e:React.FormEvent){e.preventDefault();if(await send("/api/school-transport/routes",rform)){setRform({name:"",startPointName:"",endPointName:"",description:""});setMessage("Route saved.")}}
async function editVehicle(v:Vehicle){const name=prompt("Vehicle name",v.name);if(!name)return;const vehicleNumber=prompt("Vehicle number",v.vehicleNumber??"");const driverName=prompt("Assigned driver name (blank to unassign)",v.driver?.name??"")?.trim()??"";const driver=drivers.find(item=>item.name.toLowerCase()===driverName.toLowerCase());if(driverName&&!driver){setMessage("Select an existing School Driver name.");return}const isActive=confirm("Keep this vehicle active?");await send(`/api/school-transport/vehicles/${v.id}`,{name,vehicleNumber,driverId:driver?.id??null,isActive},"PATCH")}
async function editRoute(r:Route){const name=prompt("Route name",r.name);if(!name)return;const startPointName=prompt("Start point",r.startPointName??"");const endPointName=prompt("End point",r.endPointName??"");const description=prompt("Description",r.description??"");const isActive=confirm("Keep this route active?");await send(`/api/school-transport/routes/${r.id}`,{name,startPointName,endPointName,description,isActive},"PATCH")}
async function createLink(vehicleId:string){const routeName=prompt("Route name for this parent link (blank for any route)",routes[0]?.name??"")?.trim()??"";const route=routes.find(item=>item.name.toLowerCase()===routeName.toLowerCase());if(routeName&&!route){setMessage("Route name was not found.");return}await send("/api/school-transport/links",{vehicleId,routeId:route?.id??null})}
const mapPoints=useMemo(()=>trips.filter(t=>t.lastLatitude!=null&&t.lastLongitude!=null).map(t=>({latitude:t.lastLatitude!,longitude:t.lastLongitude!,accuracyM:t.lastAccuracyM,heading:t.lastHeading,vehicleName:t.vehicle.name,routeName:t.route?.name,lastLocationAt:t.lastLocationAt})),[trips]);
return <div className="space-y-6"><div><p className="text-xs font-bold uppercase text-brand-600">Premium Module</p><h1 className="text-2xl font-bold">School Van Live Tracking</h1><p className="text-sm text-slate-500">Manage vans, routes, parent links and active school trips.</p></div>{message&&<div className="rounded-lg border p-3 text-sm">{message}</div>}<div className="grid gap-4 xl:grid-cols-2"><form onSubmit={addVehicle} className="space-y-3 rounded-xl border bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><h2 className="font-bold">Add School Vehicle</h2><input required placeholder="Van name" value={vform.name} onChange={e=>setVform({...vform,name:e.target.value})} className="min-h-11 w-full rounded-lg border px-3"/><input placeholder="Vehicle number" value={vform.vehicleNumber} onChange={e=>setVform({...vform,vehicleNumber:e.target.value})} className="min-h-11 w-full rounded-lg border px-3"/><select value={vform.driverId} onChange={e=>setVform({...vform,driverId:e.target.value})} className="min-h-11 w-full rounded-lg border px-3"><option value="">Unassigned driver</option>{drivers.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><button disabled={busy} className="rounded-lg bg-brand-600 px-4 py-2 font-semibold text-white">Save Vehicle</button></form><form onSubmit={addRoute} className="space-y-3 rounded-xl border bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><h2 className="font-bold">Add School Route</h2><input required placeholder="Route name" value={rform.name} onChange={e=>setRform({...rform,name:e.target.value})} className="min-h-11 w-full rounded-lg border px-3"/><div className="grid grid-cols-2 gap-2"><input placeholder="Start point" value={rform.startPointName} onChange={e=>setRform({...rform,startPointName:e.target.value})} className="min-h-11 rounded-lg border px-3"/><input placeholder="End point" value={rform.endPointName} onChange={e=>setRform({...rform,endPointName:e.target.value})} className="min-h-11 rounded-lg border px-3"/></div><textarea placeholder="Description" value={rform.description} onChange={e=>setRform({...rform,description:e.target.value})} className="w-full rounded-lg border p-3"/><button disabled={busy} className="rounded-lg bg-brand-600 px-4 py-2 font-semibold text-white">Save Route</button></form></div>
<section className="space-y-3"><h2 className="text-lg font-bold">Vehicles & Parent Links</h2>{vehicles.map(v=><article key={v.id} className="rounded-xl border bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><div className="flex flex-wrap justify-between gap-2"><div><b>{v.name}</b> <span className="text-sm text-slate-500">{v.vehicleNumber} · {v.driver?.name??"Unassigned"}</span></div><button onClick={()=>void editVehicle(v)} className="rounded border px-3 py-1 text-xs">Edit</button></div><div className="mt-3 flex flex-wrap gap-2"><button onClick={()=>void createLink(v.id)} className="rounded border px-3 py-2 text-xs font-semibold"><Link2 className="mr-1 inline h-3 w-3"/>Create Parent Link</button>{v.trackingLinks.map(link=><span key={link.id} className="inline-flex flex-wrap items-center gap-1 rounded-lg bg-slate-100 p-2 text-xs dark:bg-slate-800"><button onClick={()=>navigator.clipboard.writeText(`${location.origin}/school-track/${link.token}`)}><Copy className="h-3 w-3"/></button>{link.route?.name??"Any route"}<button onClick={()=>void send(`/api/school-transport/links/${link.id}/regenerate`)}><RefreshCw className="h-3 w-3"/></button><button onClick={()=>void send(`/api/school-transport/links/${link.id}/${link.isEnabled?"disable":"enable"}`)}>{link.isEnabled?"Disable":"Enable"}</button></span>)}</div></article>)}</section>
<section className="space-y-3"><div className="flex items-center justify-between"><h2 className="text-lg font-bold">Active Vans</h2><button onClick={()=>void load()}>{busy?<Loader2 className="h-4 w-4 animate-spin"/>:<RefreshCw className="h-4 w-4"/>}</button></div><MapplsLiveMap latitude={mapPoints[0]?.latitude} longitude={mapPoints[0]?.longitude} isLive locations={mapPoints} height="380px"/ ><div className="grid gap-3 md:grid-cols-2">{trips.map(t=><div key={t.id} className="rounded-xl border p-3 text-sm"><b>{t.vehicle.name}</b><p>{t.route?.name??"No route"} · {t.driver.name}</p><p className="text-slate-500">Last updated {t.lastLocationAt?new Date(t.lastLocationAt).toLocaleTimeString():"waiting for GPS"} · {t.lastAccuracyM?`${Math.round(t.lastAccuracyM)}m`:"-"}</p></div>)}</div></section><section><h2 className="font-bold">Routes</h2>{routes.map(r=><button key={r.id} onClick={()=>void editRoute(r)} className="mr-2 mt-2 rounded-lg border px-3 py-2 text-sm">{r.name} · Edit</button>)}</section></div>}
